<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alfredomartinromo.locationservice">

    //dar permisos de localización
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service android:name=".Service.Services.Localizacion">
            <intent-filter>
                <action android:name="com.example.onboot.MyService">
                </action></intent-filter>
        </service>

        <service
            android:name=".Service.Services.LecturaSonda"
            android:exported="false"></service>


        <receiver android:name=".Service.Receivers.ReceiverBoot">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED">
                    <category android:name="android.intent.category.HOME">
                    </category></action></intent-filter>
        </receiver>
    </application>

</manifest>